## Agent Instructions

- Never run builds or test runs (for example `xcodebuild`, `swift test`, `fastlane`, `make test`).
- The user runs all builds/tests manually.
- If build/test verification is needed, provide exact commands for the user instead of executing them.
